Path Editor
Selection Rectangle
Selection add to and remove from -> SHIFT click 

Create objects: circles, rects, paths, lines
- creation dialog of default then normal settings
- select new object, so dialogs are visible and object is visible
- make subtree a type and add it to list of creatable objects
- SHIFT drag creates a copy (or ref?)

Delete objects

Load file, let user select
Import file, multi file support

Copy, Cut, Paste
Drag & Drop

Split screen/Merge Screen for TabsView



click: new selection, deselect previous
drag: pan viewport of if
⇧-click: toggle selection, add if wasn't selected, remove if it was
⌥-drag: new selection with selection rectangle
⇧⌥-drag: toggle selection with selection rectangle